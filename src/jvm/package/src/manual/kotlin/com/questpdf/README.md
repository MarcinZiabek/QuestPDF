# Handwritten sources

This source set hosts hand-written implementations for members excluded from
generation via `manual-overrides.txt` (one doc-comment ID per line, at the
repository root). The generator never writes into this directory.
